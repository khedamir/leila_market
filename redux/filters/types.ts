export interface FiltersSliceState {
  menu: number;
  // collection: number | null;
  category: number | null;
  // size: string[];
  // color: string[];
  min_price: number;
  max_price: number;
  // sort: any; //
  // search: string;
  page: number;
}
